#include <stdio.h>
#include <stdlib.h>

int main()
{
    int n, s[100001], m[100001], i, *m, *s;
    scanf("%d", &n);
    for(i = 0; i <= n; i++)
    {
        scanf("%d %d", &m[i], &s[i]);
    }


    return 0;
}
